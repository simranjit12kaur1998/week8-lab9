<!-- updatepayment.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Update Payment</title>
  <link rel="stylesheet" href="/css/styles.css" />
  <!-- Link to external stylesheet -->
</head>
<body>
  <nav class="navbar">
    <h1>My Payments</h1>
    <div class="links">
      <a href="/">Home</a>
      <a class="new-item-link" href="/addpayment">New Payment</a>
    </div>
  </nav>

  <div class="content">
    <div class="update">
      <h2>Update Payment</h2>
      <form action="/single-payment/<%= payment._id %>?_method=PUT" method="POST">
        <label>Title:</label>
        <input type="text" value="<%= payment.title %>" id="title" name="title" required />

        <label for="description">Description:</label>
        <textarea id="description" name="description" rows="4" required><%= payment.description %></textarea>

        <label for="targetDate">Target Date:</label>
        <input type="date" id="targetDate" name="targetDate" required value="<%= payment.targetDate.toISOString().substr(0, 10) %>" />

        <label>Author:</label>
        <select name="author">
          <option value="sami">Sami</option>
          <option value="rami">Rami</option>
        </select>

        <label for="achieved">Achieved:</label>
        <input type="checkbox" id="achieved" name="achieved" <% if (payment.achieved) { %>checked<% } %> />

        <button type="submit">Update Payment</button>
      </form>
    </div>
  </div>
</body>
</html>